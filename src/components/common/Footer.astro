---

---

<footer class="flex justify-between items-center w-full h-[256px]">
  <span class="flex flex-col gap-4 items-start w-full">
    <!-- <p class="text-center text-sm">
      <a href="mailto:kizzendev@gmail.com">Contact</a>
    </p> -->
    <p class="text-center text-sm">
      <a href="/legal">Legal</a>
    </p>
    <p class="text-center text-sm">
      &copy; 2024 Kizendev • All rights reserved.
    </p>
  </span>

  <!-- <span class="grid place-items-center w-1/4 h-full">
    <div id="twitch-embed" class="w-full h-full"></div>
    <div id="not-twitch-embed" class="hidden">Hello</div>
  </span> -->
</footer>

<!-- <script src="https://embed.twitch.tv/embed/v1.js" is:inline></script> -->

<!-- <script type="text/javascript" is:inline>
  const options = {
    channel: 'kizendev',
    width: '100%',
    height: '100%',
    channel: 'kizendev',
    allowfullscreen: true,
    autoplay: true,
    muted: false,
    theme: 'dark',
    layout: 'video',
  };

  // const embed = new Twitch.Embed('twitch-embed', options);

  const notTwitchEmbed = document.getElementById('not-twitch-embed');
  const twitchEmbed = document.getElementById('twitch-embed');

  function streamOnline() {
    if (twitchEmbed.classList.contains('hidden')) {
      twitchEmbed.classList.remove('hidden');
      notTwitchEmbed.classList.add('hidden');
    }
  }

  function streamOffline() {
    if (notTwitchEmbed.classList.contains('hidden')) {
      notTwitchEmbed.classList.remove('hidden');
      twitchEmbed.classList.add('hidden');
    }
  }

  // embed.addEventListener(Twitch.Embed.READY, initiate);

  function initiate() {
    embed.addEventListener(Twitch.Embed.ONLINE, handleOnline);
    embed.addEventListener(Twitch.Embed.OFFLINE, handleOffline);
    embed.removeEventListener(Twitch.Embed.READY, initiate);
  }

  function handleOnline() {
    streamOnline();
    embed.removeEventListener(Twitch.Embed.ONLINE, handleOnline);
    embed.addEventListener(Twitch.Embed.OFFLINE, handleOffline);
    embed.setMuted(false);
  }

  function handleOffline() {
    streamOffline();
    embed.removeEventListener(Twitch.Embed.OFFLINE, handleOffline);
    embed.addEventListener(Twitch.Embed.ONLINE, handleOnline);
    embed.setMuted(true);
  }
</script> -->
