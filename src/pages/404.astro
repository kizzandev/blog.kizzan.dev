---

---

<!doctype html>
<html lang="es-AR" class="">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="La página a la que se quiso acceder no existe."
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Error 404 | Página no encontrada</title>
  </head>

  <body>
    <section
      class="grid place-content-center bg-zinc-900 min-h-screen text-zinc-100"
    >
      <div class="space-y-4 flex flex-col">
        <h1 class="text-8xl font-semibold text-center">404</h1>
        <p class="text-2xl text-center">La página que buscas no existe.</p>
        <a
          href={`/`}
          class="inline-block self-center text-center px-6 py-3 text-xl font-semibold bg-zinc-800 rounded-md hover:bg-zinc-700"
        >
          Volver al inicio
        </a>
        <!-- <button
          class="inline-block self-center text-center px-6 py-3 text-xl font-semibold bg-zinc-800 rounded-md hover:bg-zinc-700"
          onclick="window.history.back()">Volver atrás</button
        > -->
      </div>
    </section>
  </body>
</html>
